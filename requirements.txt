# MCP server
mcp[cli]>=1.0.0
fastmcp>=3.0.0

# Vector embeddings — pulls in torch, numpy, transformers, huggingface-hub automatically
# Model all-MiniLM-L6-v2 (~90MB) is downloaded from HuggingFace on first use,
# then cached at ~/.cache/huggingface/hub/ for offline use.
# Run: python memory_manager.py memory-warmup   (once, after pip install)
sentence-transformers>=3.0.0

# Storage
# sqlite-vec is installed but not used for ANN search (cosine similarity runs in NumPy
# over BLOB storage, which is fast enough for project-scale memory sets).
# Kept here as a drop-in upgrade path if ANN indexing is needed at larger scale.
sqlite-vec>=0.1.0

# Graph, cache, validation
networkx>=3.0
pydantic>=2.0.0
cachetools>=5.0.0

# Config and CLI
python-dotenv>=1.0.0
typer>=0.12.0
rich>=13.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
